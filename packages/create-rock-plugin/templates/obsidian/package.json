{
  "name": "{{ orgCode }}.{{ pluginCode | downcase }}.obsidian",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}